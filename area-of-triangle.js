let baseValue = prompt("Enter first value: ");
let baseHeight = prompt("Enter second value: ");
let totalArea = .5 * (baseValue * baseHeight);

console.log(`Area of triangle result is: ${totalArea}`);